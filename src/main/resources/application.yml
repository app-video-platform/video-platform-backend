spring:
  application:
    name: video-platform
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5430}/${DB_NAME:videoplatform}
    username: ${DB_USER:tamastyukodi}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: true

sendgrid:
  api:
    key: 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
  mail:
    from: "no-reply@serious-debauchery.click"


server:
  forward-headers-strategy: native

app:
  domain: "serious-debauchery.netlify.app"
  frontend:
    url: "https://serious-debauchery.netlify.app"

jwt:
  secret: "12345678901234567890123456789012"
  expiration: 900000
